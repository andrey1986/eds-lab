version: 1

indices:
  default:
    include:
      - /**

    properties:
      title:
        selectFirst: head > title
        value: textContent(el)

      description:
        selectFirst: head > meta[name="description"]
        value: attribute(el, "content")

      tags:
        selectFirst: head > meta[name="tags"]
        value: attribute(el, "content")
